/**
 * _isupper - check if l is uppercase
 * @l: input alphabet to be checked
 * Return: 1 if its uppercase, 0 if not
*/

int _isupper(int l)
{
	if (l >= 'a' && l <= 'z')
		return (1);

	return (0);

}
