/**
 * _isupper - check if l is uppercase
 * @c: input alphabet to be checked
 * Return: 1 if its uppercase, 0 if not
*/

int _isupper(int c)
{
	if (c >= 'a' && c <= 'z')
		return (1);

	return (0);

}
