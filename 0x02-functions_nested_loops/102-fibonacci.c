
#include <stdio.h>

/**
 * main - prints out fibonacci numbers
 * Return:0
**/

int main(void)
{
	int preNum = 0, num = 1;

	while (preNum <= 50)
	{
	
	num += preNum;
	//fib = num + preNum;
	printf("%d", fib);
	printf(",");
	printf(" ");
	fib = num 
	preNum = num;
	}
	return (0);
}
